name: opencloset_database
description: CSV-based storage layer for local persistence in OpenCloset.

homepage: https://github.com/sophie-nayebi/OpenCloset
repository: https://github.com/sophie-nayebi/OpenCloset/tree/main/packages/database

environment:
  sdk: ">=3.0.0 <4.0.0"

sources:
  - lib

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.2

dev_dependencies:
  build_runner: ^2.15.1
  flutter_test:
    sdk: flutter
  test: ^1.22.0
  matcher: ^0.12.19
