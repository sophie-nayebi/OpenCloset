name: opencloset_image_processing
description: Image processing — background removal, masking, and thumbnails for OpenCloset.

version: 0.0.1
homepage: https://github.com/sophie-nayebi/OpenCloset

publish_to: "none"

repository: https://github.com/sophie-nayebi/OpenCloset/tree/main/packages/image_processing

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter: { sdk: flutter }
  image: ^0.4.5
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test: { sdk: flutter }
